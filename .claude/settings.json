{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); if printf '%s' \"$cmd\" | grep -q salloc; then echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"salloc creates a Slurm allocation (shared HPC time) — explicit approval required; never auto-run.\"}}'; fi",
            "statusMessage": "Checking for salloc guard"
          }
        ]
      }
    ]
  }
}
